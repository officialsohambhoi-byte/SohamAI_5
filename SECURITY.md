# Security

- Never commit API keys, passwords, tokens, keystores, or other secrets.
- Production AI provider credentials must remain server-side.
- Use HTTPS for backend communication.
- Enforce authentication and per-user authorization on the backend.
- Validate uploaded files and tool inputs server-side.
- Do not execute arbitrary user code on the Android device.
